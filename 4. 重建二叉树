# ——————题目描述——————：
# 输入某二叉树的前序遍历和中序遍历的结果，请重建出该二叉树。假设输入的前序遍历和中序遍历的结果中都不含重复的数字。
# 例如输入前序遍历序列{1,2,4,7,3,5,6,8}和中序遍历序列{4,7,2,1,5,3,8,6}，则重建二叉树并返回。

# ——————解题思路——————：
# 前序遍历的第一个元素是二叉树的根节点，寻找其在中序遍历中的位置 pos，中序遍历数组中 pos 左边的为二叉树的左子树，右边为二叉树的右子树。
# 同时根据左子树和右子树的元素个数可以确定左子树的前序遍历和右子树的前序遍历，然后分别递归求解。


# -*- coding:utf-8 -*-
# class TreeNode:
#     def __init__(self, x):
#         self.val = x
#         self.left = None
#         self.right = None
class Solution:
    # 返回构造的TreeNode根节点
    def reConstructBinaryTree(self, pre, tin):
        # write code here
        if not pre or not tin:
            return None
        
        root = TreeNode(pre[0])    # 前序遍历的第一个元素是根结点
        pos = tin.index(pre[0])    # pos 为 根结点在中序遍历中的位置
        
        root.left = self.reConstructBinaryTree(pre[1:pos+1], tin[:pos])
        root.right = self.reConstructBinaryTree(pre[pos+1:], tin[pos+1:])
        
        return root
